{
  "name": "uongbigo",
  "version": "1.0.0",
  "private": true,
  "description": "Uong Bi GO deployed with Vercel Functions and Neon Postgres",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  },
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel",
    "deploy:prod": "npx vercel --prod",
    "test": "node tests/health.test.js"
  }
}
