# Neon Postgres connection string. Vercel injects it after Neon is connected.
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# A long, unique random secret used to sign login sessions.
AUTH_SECRET="replace-with-a-long-random-secret"
